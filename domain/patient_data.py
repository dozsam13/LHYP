


class PatientData:
    def __init__(self, scan_id, pathology, volume_data, contour_diff_matricies):
    	self.scan_id = scan_id
    	self.pathology = pathology
    	self.volume_data = volume_data
    	self.contour_diff_matricies = contour_diff_matricies